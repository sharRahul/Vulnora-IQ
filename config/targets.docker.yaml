# VulnoraIQ Docker Target Configuration
# -------------------------------------
# This file is used by the Docker Compose deployment (VULNORAIQ_TARGET_CONFIG=targets.docker.yaml).
# Configure your own authorised AI application targets here.
# No mock or demo targets are included by default.

targets: {}
